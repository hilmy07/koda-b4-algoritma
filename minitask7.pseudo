INPUT R
Phi = 0

IF R % 7 == 0 THEN
    Phi = 22/7
ELSE
    Phi = 3.14
ENDIF

PROCEDURE Luas(Phi, R)
    Luas = Phi * R * R
    OUTPUT Luas
ENDPROCEDURE

PROCEDURE Keliling(Phi, R)
    Keliling = 2 * Phi * R
    OUTPUT Keliling
ENDPROCEDURE

Luas(Phi, R)
Keliling(Phi, R)
