input N

PROCEDURE IsEven(Number)
    IF Number%2==0 THEN
        RETURN true
    ELSE 
        RETURN false
    ENDIF
ENDPROCEDURE

IF IsEven(N) THEN
    OUTPUT "GENAP"
ELSE
    OUTPUT "GANJIL"
ENDIF
